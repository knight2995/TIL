#pragma once
//형식상 분류를 위해 _를 함수명 앞에 붙임

int _strlen(const char *str);
int _strcmp(const char* str1, const char* str2);
char* _strchr(char* str, char c);
char* _strcpy(char* dest, char *src);
char* _strcat(char* dest, char *src);